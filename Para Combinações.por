programa {
  funcao inicio() {
    inteiro contadorUm, contadorDois
    para(contadorUm = 1; contadorUm <= 3; contadorUm ++){
      para(contadorDois = 1; contadorDois <=3; contadorDois ++){
        escreva(" " + contadorUm + ", " + contadorDois + "|")
      }
    }
  }
}
